# The app is fully functional WITHOUT any API key — these are optional.
# Setting either one enables the Phase 3 AI explanation layer (plain-English
# summary of the deterministic findings). The AI layer only explains the
# facts the engine already found; it never decides the verdict.

# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
